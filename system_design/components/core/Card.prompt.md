Contentor padrao. Borda hairline sempre; sombra apenas em `raised` ou quando `href` deixa o cartao clicavel.

```jsx
<Card variant="raised"><h3>Valuation</h3><p>...</p></Card>
<Card href="/casos/industria" variant="default">...</Card>
```

`dark` para blocos sobre grafite (ja inverte a cor do texto).
