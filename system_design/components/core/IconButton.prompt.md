Botao de um unico icone — navegacao de carrossel, fechar dialogo, download.

```jsx
<IconButton name="download" label="Baixar PDF" />
<IconButton name="arrow-right" label="Proximo" variant="solid" />
```

Sempre informe `label`. Nunca use como acao primaria de pagina.
