Troca de vista dentro de uma mesma pagina (ex. servicos por frente de trabalho).

```jsx
<Tabs items={['M&A','Controladoria','Funding','Valuation']} onChange={setV} />
```

Maximo ~5 abas; acima disso use uma lista lateral.
